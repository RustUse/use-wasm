# Scripts

Repository scripts can live here when a task becomes too large for Cargo aliases or the Makefile.

The initial scaffold keeps validation in Cargo aliases, GitHub Actions, and the Makefile.
